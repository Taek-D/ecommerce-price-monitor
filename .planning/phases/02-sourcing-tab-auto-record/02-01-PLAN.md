---
phase: 02-sourcing-tab-auto-record
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - coupang_manager.py
  - tests/test_sourcing_tab.py
autonomous: true
requirements:
  - SMAP-01
  - SMAP-02

must_haves:
  truths:
    - "vendorItemId로 소싱목록에서 구매링크(D열)와 매입가격(H열)이 함께 조회된다"
    - "URL 도메인 문자열이 올바른 소싱처 탭 이름으로 변환된다"
    - "알려지지 않은 도메인은 None을 반환한다"
    - "소싱목록에 없는 vendorItemId는 빈 결과를 반환한다"
  artifacts:
    - path: "config.py"
      provides: "DOMAIN_TO_SOURCING_TAB dict"
      contains: "DOMAIN_TO_SOURCING_TAB"
    - path: "coupang_manager.py"
      provides: "_load_sourcing_info_by_vid function"
      contains: "def _load_sourcing_info_by_vid"
    - path: "coupang_manager.py"
      provides: "_resolve_sourcing_tab_name function"
      contains: "def _resolve_sourcing_tab_name"
    - path: "tests/test_sourcing_tab.py"
      provides: "Unit tests for mapping and lookup"
      min_lines: 60
  key_links:
    - from: "coupang_manager.py:_load_sourcing_info_by_vid"
      to: "소싱목록 sheet columns D, H, O"
      via: "_open_coupang_sheet + row iteration"
      pattern: "_load_sourcing_info_by_vid"
    - from: "coupang_manager.py:_resolve_sourcing_tab_name"
      to: "config.py:DOMAIN_TO_SOURCING_TAB"
      via: "urlparse domain lookup"
      pattern: "DOMAIN_TO_SOURCING_TAB"
---

<objective>
Define the data layer for sourcing tab auto-recording: domain-to-tab mapping config, vendorItemId-to-sourcing-info lookup function, and URL-to-tab-name resolver.

Purpose: Provide the foundational lookup functions that Plan 02 will call from process_new_orders() to record orders in sourcing tabs. These are pure/IO-isolated functions that can be fully unit-tested.
Output: DOMAIN_TO_SOURCING_TAB dict in config.py, two new functions in coupang_manager.py, comprehensive unit tests.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

Key codebase references (do NOT re-explore — use these directly):

<interfaces>
From coupang_manager.py — existing patterns to follow:

```python
# Sheet constants (line 1613-1621)
SOURCING_SHEET = "소싱목록"
SOURCING_HEADER_ROW = 2
SOURCING_DATA_START = 3
SOURCING_COL_NAME = 2    # B열: 상품명
SOURCING_COL_BUYPRICE = 8  # H열: 매입가격
SOURCING_COL_VID = 15     # O열: vendorItemId(쿠팡)

# Sheet helper (line 415-418)
def _open_coupang_sheet(sheet_name: str):
    gc = gspread.authorize(_google_creds())
    sh = gc.open_by_key(COUPANG_SHEET_ID)
    return sh.worksheet(sheet_name)

# VID parser (line 596-618)
def _parse_vendor_item_ids(raw_value: str) -> list[str]: ...

# Existing sourcing info loader pattern (line 637-670)
def _load_sourcing_min_price_by_vid() -> dict[str, int]:
    """소싱목록 O열(vendorItemId) -> K열(최소판매금액) 인덱스 생성."""

# Sourcing tabs list (line 3651-3661)
_SOURCING_ORDER_TABS = [
    "무신사", "hmall", "복지몰", "올리브영", "11번가",
    "네이버", "지마켓", "옥션", "sk스토아",
]
```

From PROJECT.md — sourcing sheet column structure:
- 소싱목록 탭: B=상품명, D=구매링크(URL), H=매입가격, O=vendorItemId(쿠팡)
- 소싱처 탭: A=구매날짜, B=주문자명, G=상품명, H=수량, I=구매처(URL), L=판매가격, M=매입가격
- 소싱처 탭 목록: 무신사, 11번가, 지마켓, 옥션, 네이버, hmall, 올리브영, 복지몰, sk스토아, 사입
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add domain-to-tab mapping and sourcing lookup functions</name>
  <files>config.py, coupang_manager.py, tests/test_sourcing_tab.py</files>
  <behavior>
    - DOMAIN_TO_SOURCING_TAB maps known domains to tab names:
      - "musinsa.com" -> "무신사"
      - "gmarket.co.kr" -> "지마켓"
      - "11st.co.kr" -> "11번가"
      - "auction.co.kr" -> "옥션"
      - "naver.com" -> "네이버" (smartstore included)
      - "hmall.com" -> "hmall"
      - "oliveyoung.co.kr" -> "올리브영"
      - "sk스토아" domains -> "sk스토아"
      - "복지몰" domains -> "복지몰"
    - _resolve_sourcing_tab_name("https://www.musinsa.com/products/123") returns "무신사"
    - _resolve_sourcing_tab_name("https://item.gmarket.co.kr/Item?id=1") returns "지마켓"
    - _resolve_sourcing_tab_name("https://smartstore.naver.com/shop/products/1") returns "네이버"
    - _resolve_sourcing_tab_name("https://unknown-site.com/x") returns None
    - _resolve_sourcing_tab_name("") returns None
    - _load_sourcing_info_by_vid returns dict[str, dict] mapping vid to {"url": str, "buy_price": int, "product_name": str}
    - vid present in O열 with valid D열 URL and H열 price -> returns all three fields
    - vid present but D열 empty -> url is "" (still returns entry)
    - vid not in sourcing list -> not in result dict
    - Multiple vids in one O열 cell -> each vid maps to same row's data
  </behavior>
  <action>
1. In `config.py`, add `DOMAIN_TO_SOURCING_TAB` dict at the end (before the Settings class). Map domain substrings to tab names. Include all 10 sourcing tabs from PROJECT.md. Use domain suffix matching (e.g., "musinsa.com" matches "www.musinsa.com"). Also add the "사입" tab with no domain mapping (manual-only tab).

```python
DOMAIN_TO_SOURCING_TAB: dict[str, str] = {
    "musinsa.com": "무신사",
    "gmarket.co.kr": "지마켓",
    "11st.co.kr": "11번가",
    "auction.co.kr": "옥션",
    "naver.com": "네이버",
    "hmall.com": "hmall",
    "oliveyoung.co.kr": "올리브영",
    "skstoa.com": "sk스토아",
    "ezwel.com": "복지몰",
}
```

2. In `coupang_manager.py`, add a new constant `SOURCING_COL_URL = 4` (D열: 구매링크) near the existing SOURCING_COL constants (line ~1617).

3. In `coupang_manager.py`, add function `_resolve_sourcing_tab_name(url: str) -> str | None` near the sourcing section (~line 1628). It should:
   - Parse URL with `urlparse` to get hostname
   - Iterate over `DOMAIN_TO_SOURCING_TAB` keys
   - Return tab name if hostname ends with any key
   - Return None if no match
   - Handle empty/invalid URLs gracefully (return None)

4. In `coupang_manager.py`, add function `_load_sourcing_info_by_vid() -> dict[str, dict]` right after `_load_sourcing_min_price_by_vid()` (after line 670). Follow the exact same pattern:
   - Open SOURCING_SHEET via `_open_coupang_sheet(SOURCING_SHEET)`
   - Get all rows via `ws.get_all_values()`
   - Iterate from `SOURCING_DATA_START` onward
   - For each row, read O열 (vid), D열 (url), H열 (buy_price), B열 (product_name)
   - Use `_parse_vendor_item_ids()` for O열 parsing (supports multiple vids per cell)
   - Use `_to_positive_int()` for H열 buy_price parsing
   - Return `dict[str, dict]` where key=vid, value={"url": str, "buy_price": int|None, "product_name": str}
   - If same vid appears in multiple rows, keep the first occurrence (unlike min_price which takes max)
   - Log sourcing info load count at info level

5. Create `tests/test_sourcing_tab.py` with unit tests:
   - Test `_resolve_sourcing_tab_name` for all domain mappings, unknown domain, empty string, None-like input
   - Test `DOMAIN_TO_SOURCING_TAB` has entries for all `_SOURCING_ORDER_TABS` (except "사입" which has no URL domain)
   - Test `_load_sourcing_info_by_vid` by mocking `_open_coupang_sheet` to return a mock worksheet with `get_all_values()` returning sample rows. Verify vid->info mapping, multi-vid cells, missing URL/price handling.
   - Use `pytest` + `unittest.mock.patch` pattern (matching existing test style in test_coupang_utils.py)
  </action>
  <verify>
    <automated>cd E:/musinsa-bot && python -m pytest tests/test_sourcing_tab.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - DOMAIN_TO_SOURCING_TAB dict exists in config.py with all 9 domain->tab mappings
    - _resolve_sourcing_tab_name() correctly maps URLs to tab names, returns None for unknown
    - _load_sourcing_info_by_vid() returns vid->{"url","buy_price","product_name"} dict
    - SOURCING_COL_URL = 4 constant defined
    - All tests pass
  </done>
</task>

</tasks>

<verification>
```bash
cd E:/musinsa-bot && python -m pytest tests/test_sourcing_tab.py -x -v
```
- All domain mapping tests pass
- All sourcing info loader tests pass (mocked sheet)
- `python -c "from config import DOMAIN_TO_SOURCING_TAB; print(len(DOMAIN_TO_SOURCING_TAB))"` prints 9
- `python -c "from coupang_manager import _resolve_sourcing_tab_name; print(_resolve_sourcing_tab_name('https://www.musinsa.com/products/1'))"` prints "무신사"
</verification>

<success_criteria>
- DOMAIN_TO_SOURCING_TAB contains mappings for all 9 sourcing tab domains
- _resolve_sourcing_tab_name converts any known sourcing URL to the correct tab name
- _load_sourcing_info_by_vid loads vid -> {url, buy_price, product_name} from sourcing sheet
- All unit tests in test_sourcing_tab.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-sourcing-tab-auto-record/02-01-SUMMARY.md`
</output>
