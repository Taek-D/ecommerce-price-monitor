---
phase: 04-db-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - db.py
  - requirements.txt
  - tests/test_db.py
autonomous: true
requirements:
  - DB-01
  - DB-02
  - DB-04

must_haves:
  truths:
    - "open_db() creates a singleton aiosqlite connection with WAL mode active"
    - "close_db() cleanly closes the connection and sets _conn to None"
    - "get_conn() raises RuntimeError when called before open_db()"
    - "init_schema() creates all 7 tables (6 data + schema_version)"
    - "schema_version table is seeded with version=1 on first init"
    - "DB_FILE constant exists in config.py as absolute path"
  artifacts:
    - path: "db.py"
      provides: "DB singleton module — open_db, close_db, get_conn, init_schema, _write_lock"
      exports: ["open_db", "close_db", "get_conn", "_write_lock"]
    - path: "tests/test_db.py"
      provides: "Unit tests for all db.py functions"
      min_lines: 50
    - path: "config.py"
      provides: "DB_FILE constant"
      contains: "DB_FILE"
    - path: "requirements.txt"
      provides: "aiosqlite dependency"
      contains: "aiosqlite"
  key_links:
    - from: "db.py"
      to: "config.py"
      via: "from config import DB_FILE"
      pattern: "from config import DB_FILE"
    - from: "db.py:open_db"
      to: "db.py:init_schema"
      via: "await init_schema() called at end of open_db()"
      pattern: "await init_schema"
---

<objective>
Create the db.py module with aiosqlite singleton connection, WAL mode, 7-table schema, and comprehensive tests.

Purpose: Establish the DB infrastructure layer that Phase 5 (Event Logging) and Phase 6 (Migration) will build upon. No data reads/writes in this phase -- pure infrastructure.
Output: db.py module, test_db.py tests, DB_FILE constant in config.py, aiosqlite in requirements.txt
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-db-foundation/04-CONTEXT.md
@.planning/phases/04-db-foundation/04-RESEARCH.md

@config.py
@requirements.txt
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From config.py (line 13):
```python
_PROJECT_ROOT = Path(__file__).resolve().parent
```

From config.py (line 23):
```python
STATE_FILE = "price_state.json"
# DB_FILE will be added next to this line
```

From pyproject.toml:
```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add aiosqlite dependency + DB_FILE constant + create db.py with tests</name>
  <files>requirements.txt, config.py, db.py, tests/test_db.py</files>
  <behavior>
    - Test: open_db() sets db._conn to a non-None connection object; calling open_db() twice is idempotent (no error)
    - Test: close_db() sets db._conn to None; calling close_db() when already closed is safe (no error)
    - Test: get_conn() raises RuntimeError with "open_db" in message when _conn is None
    - Test: After open_db() on a file-backed DB, PRAGMA journal_mode returns "wal"
    - Test: After open_db(), all 7 tables exist in sqlite_master: schema_version, price_state, price_checks, price_events, adapter_runs, job_runs, discovery_candidates
    - Test: After open_db(), schema_version table contains exactly one row with version=1
    - Test: Calling open_db() twice then close_db() once results in _conn being None (idempotent open)
    - Test: DB_FILE is importable from config and contains "ops.db"
  </behavior>
  <action>
1. Add `aiosqlite>=0.20.0` to requirements.txt under the `# Core` section (after apscheduler line). Install it: `pip install aiosqlite>=0.20.0`.

2. In config.py, add `DB_FILE` constant on line 24 (right after `STATE_FILE = "price_state.json"`):
   ```python
   DB_FILE = str(_PROJECT_ROOT / "ops.db")
   ```
   Use absolute path via `_PROJECT_ROOT` to avoid cwd-dependent file creation (per PITFALLS.md Pitfall 4).

3. Create db.py with these functions:
   - Module-level `_conn: aiosqlite.Connection | None = None` and `_write_lock = asyncio.Lock()`
   - `async def open_db() -> None`: If _conn is not None, return (idempotent). Connect to DB_FILE via aiosqlite.connect(). Execute pragmas IN THIS ORDER before anything else: `PRAGMA journal_mode=WAL`, `PRAGMA synchronous=NORMAL`, `PRAGMA foreign_keys=ON`, `PRAGMA busy_timeout=10000`. Commit. Then call `await init_schema()`. Log "DB opened: {DB_FILE}".
   - `async def close_db() -> None`: If _conn is None, return. Await _conn.close(), set _conn = None. Log "DB closed".
   - `def get_conn() -> aiosqlite.Connection`: If _conn is None, raise RuntimeError("DB not initialized -- call open_db() first"). Return _conn.
   - `async def init_schema() -> None`: Use `conn.executescript()` with `CREATE TABLE IF NOT EXISTS` for all 7 tables. After executescript, insert schema_version=1 if table is empty: `INSERT OR IGNORE INTO schema_version(version, applied_at) VALUES (1, datetime('now'))`. Commit.

   Table schemas (exact columns per CONTEXT.md):
   - schema_version: version INTEGER NOT NULL, applied_at TEXT NOT NULL
   - price_state: url TEXT PRIMARY KEY, price INTEGER, updated_at TEXT NOT NULL
   - price_checks: id INTEGER PRIMARY KEY, url TEXT NOT NULL, price INTEGER, kind TEXT NOT NULL, checked_at TEXT NOT NULL
   - price_events: id INTEGER PRIMARY KEY, url TEXT NOT NULL, old_price INTEGER, new_price INTEGER, event_type TEXT NOT NULL, detected_at TEXT NOT NULL
   - adapter_runs: id INTEGER PRIMARY KEY, adapter TEXT NOT NULL, url TEXT NOT NULL, error TEXT, traceback TEXT, run_at TEXT NOT NULL
   - job_runs: id INTEGER PRIMARY KEY, job_name TEXT NOT NULL, started_at TEXT NOT NULL, finished_at TEXT, status TEXT NOT NULL, error TEXT
   - discovery_candidates: id INTEGER PRIMARY KEY, source TEXT NOT NULL, name TEXT, url TEXT NOT NULL, price INTEGER, margin_pct REAL, score REAL, discovered_at TEXT NOT NULL

   Use `logging.getLogger("musinsa_bot.db")` for log messages.

4. Create tests/test_db.py using `monkeypatch` + `tmp_path` fixture. Each test must reset db._conn to None and monkeypatch db.DB_FILE to a tmp_path file. IMPORTANT: WAL mode does NOT work on :memory: DBs -- use file-backed tmp_path DBs for all tests. Every test must call close_db() in cleanup.

   Write tests for all behaviors listed in the behavior section above.

5. After writing tests, run RED phase: `python -m pytest tests/test_db.py -x -q` -- tests should fail (db.py not yet created, or if created simultaneously, should pass).

6. Run GREEN phase: ensure all tests pass with `python -m pytest tests/test_db.py -x -q`.

7. Run full test suite to confirm no regressions: `python -m pytest tests/ -x -q`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_db.py -x -q</automated>
  </verify>
  <done>
    - aiosqlite>=0.20.0 in requirements.txt and importable
    - config.DB_FILE exists and equals absolute path ending in "ops.db"
    - db.py exports open_db, close_db, get_conn, _write_lock
    - All 8 tests in test_db.py pass
    - Full test suite (`python -m pytest tests/ -x -q`) passes with no regressions
  </done>
</task>

</tasks>

<verification>
python -m pytest tests/test_db.py -x -q
python -m pytest tests/ -x -q
python -c "from config import DB_FILE; assert 'ops.db' in DB_FILE; print('DB_FILE OK:', DB_FILE)"
python -c "import db; print('db.py imports OK')"
python -c "import aiosqlite; print('aiosqlite version:', aiosqlite.__version__)"
</verification>

<success_criteria>
- aiosqlite installed and in requirements.txt
- DB_FILE constant in config.py is an absolute path to ops.db
- db.py module exists with open_db/close_db/get_conn/init_schema/_write_lock
- 7 tables created by init_schema (schema_version seeded with version=1)
- WAL mode confirmed on file-backed DB
- All tests pass, no regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/04-db-foundation/04-01-SUMMARY.md`
</output>
