---
phase: 01-discord
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - coupang_manager.py
autonomous: true
requirements:
  - SHIP-01

must_haves:
  truths:
    - "sync_delivery_status_to_sheet() 완료 후 상품준비중 주문이 1건 이상이면 Discord embed가 전송됨"
    - "embed에 각 주문의 주문ID와 상품명이 포함됨"
    - "상품준비중 주문이 0건이면 Discord에 아무 메시지도 전송되지 않음"
    - "COUPANG_ORDER_WEBHOOK 환경변수에 설정된 웹훅 URL로 알림이 전송됨"
  artifacts:
    - path: "coupang_manager.py"
      provides: "_notify_pending_preparation() helper + sync_delivery_status_to_sheet() 끝에서 호출"
      contains: "_notify_pending_preparation"
  key_links:
    - from: "sync_delivery_status_to_sheet()"
      to: "_notify_pending_preparation()"
      via: "함수 끝 await 호출 (로그 출력 직후)"
      pattern: "await _notify_pending_preparation"
    - from: "_notify_pending_preparation()"
      to: "post_webhook(COUPANG_ORDER_WEBHOOK, ...)"
      via: "embed 목록이 1건 이상인 경우에만 호출"
      pattern: "await post_webhook\\(COUPANG_ORDER_WEBHOOK"
---

<objective>
sync_delivery_status_to_sheet() 실행 완료 후, 현재 "상품준비중" 상태인 주문 목록을 Discord embed로 자동 알림.

Purpose: 수동으로 Google Sheets를 확인하지 않아도 상품준비중 주문 현황을 즉시 파악할 수 있도록 함.
Output: coupang_manager.py에 _notify_pending_preparation() 헬퍼 추가 + sync_delivery_status_to_sheet() 끝에서 호출.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- 이 플랜에서 사용하는 기존 함수/상수. 탐색 없이 그대로 사용할 것. -->

From coupang_manager.py:

```python
# 상수
COUPANG_ORDER_WEBHOOK: str  # os.getenv("COUPANG_ORDER_WEBHOOK", "")
COL_ORDER_ID = 1            # A열: 주문ID
COL_ORDER_PRODUCT = 2       # B열: 상품명
COL_ORDER_STATUS = 7        # G열: 배송상태
ORDER_START_ROW: int        # 데이터 시작 행 번호

# 유틸
def _now_kst_str() -> str: ...
async def post_webhook(url: str, content: str, embeds=None) -> None: ...

# sync_delivery_status_to_sheet() 함수 내부에서 구축된 변수 (함수 끝에 이미 존재)
rows: list[list[str]]                    # ws.get_all_values() 결과
order_status_by_id: dict[str, str]       # {order_id: current_status_string}
order_row_by_id: dict[str, int]          # {order_id: sheet_row_index}

# 기존 embed 패턴 (신규 주문 embed 참고)
embeds = [
    {
        "title": "🛍️ 신규 주문 접수",
        "color": 3447003,  # 파란색
        "fields": [
            {"name": "주문 ID", "value": order_id, "inline": True},
            {"name": "상품", "value": product_name, "inline": True},
            ...
            {"name": "처리시각", "value": _now_kst_str(), "inline": False},
        ],
    }
]
await post_webhook(COUPANG_ORDER_WEBHOOK, "새 주문 접수", embeds=embeds)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _notify_pending_preparation() 헬퍼 구현</name>
  <files>coupang_manager.py</files>
  <behavior>
    - 상품준비중 주문 0건: post_webhook 호출 없음
    - 상품준비중 주문 1건: embed 1개, fields에 주문ID + 상품명 포함
    - 상품준비중 주문 25건 이상: Discord embed field 한도(25)를 초과하지 않도록 truncation 처리 — 최대 24건 표시 후 "외 N건 더..." field 추가
    - embed title: "📦 상품준비중 주문 현황", color: 16776960 (노란색, 주의 상태를 표현)
    - 각 field: name=주문ID, value=상품명, inline=False
    - 마지막 field: name="확인시각", value=_now_kst_str(), inline=False
    - COUPANG_ORDER_WEBHOOK이 빈 문자열이면 post_webhook 내부에서 처리되므로 별도 체크 불필요
  </behavior>
  <action>
    sync_delivery_status_to_sheet() 함수 정의 바로 위(line ~1360)에 다음 헬퍼를 추가한다.

    ```python
    async def _notify_pending_preparation(
        rows: list[list[str]],
        order_status_by_id: dict[str, str],
    ) -> None:
        """상품준비중 주문 목록을 Discord embed로 알림. 0건이면 전송 안 함."""
        MAX_FIELDS = 24  # Discord embed field 한도(25) - 확인시각 field 1개 예약

        pending: list[tuple[str, str]] = []  # [(order_id, product_name), ...]
        for row in rows[ORDER_START_ROW - 1 :]:
            if len(row) < COL_ORDER_STATUS:
                continue
            order_id = row[COL_ORDER_ID - 1].strip() if len(row) >= COL_ORDER_ID else ""
            if not order_id:
                continue
            status = order_status_by_id.get(order_id, row[COL_ORDER_STATUS - 1].strip())
            if status != "상품준비중":
                continue
            product_name = (
                row[COL_ORDER_PRODUCT - 1].strip()
                if len(row) >= COL_ORDER_PRODUCT
                else "(상품명 없음)"
            ) or "(상품명 없음)"
            pending.append((order_id, product_name))

        if not pending:
            return

        fields = []
        display = pending[:MAX_FIELDS]
        for oid, pname in display:
            fields.append({"name": oid, "value": pname, "inline": False})

        overflow = len(pending) - len(display)
        if overflow > 0:
            fields.append(
                {"name": "...", "value": f"외 {overflow}건 더", "inline": False}
            )

        fields.append({"name": "확인시각", "value": _now_kst_str(), "inline": False})

        embeds = [
            {
                "title": f"📦 상품준비중 주문 현황 ({len(pending)}건)",
                "color": 16776960,
                "fields": fields,
            }
        ]
        await post_webhook(COUPANG_ORDER_WEBHOOK, "상품준비중 현황", embeds=embeds)
    ```

    이후 sync_delivery_status_to_sheet() 함수 끝 로그 출력(_log_order.info("배송상태 동기화 완료...")) 바로 뒤에 다음을 추가한다:

    ```python
    await _notify_pending_preparation(rows, order_status_by_id)
    ```

    주의: order_status_by_id는 sync_delivery_status_to_sheet() 내부에서 실시간 갱신되므로(line 1468: order_status_by_id[order_id] = target_status) 함수 끝에서 읽으면 최신 상태 반영됨.
  </action>
  <verify>
    <automated>cd E:/musinsa-bot && python -c "
import ast, sys
with open('coupang_manager.py', 'r', encoding='utf-8') as f:
    src = f.read()
# 구문 오류 없음
ast.parse(src)
# 헬퍼 함수 존재
assert '_notify_pending_preparation' in src, 'helper not found'
# sync_delivery_status_to_sheet 끝에서 호출
assert 'await _notify_pending_preparation(rows, order_status_by_id)' in src, 'call not found'
print('OK: syntax valid, helper present, call present')
"
    </automated>
  </verify>
  <done>
    - coupang_manager.py 구문 오류 없음
    - _notify_pending_preparation() 함수 정의 존재
    - sync_delivery_status_to_sheet() 마지막 줄에서 await _notify_pending_preparation(rows, order_status_by_id) 호출
  </done>
</task>

<task type="auto">
  <name>Task 2: 환경변수 선언 확인 및 .env 예시 주석 추가</name>
  <files>coupang_manager.py, docs/SETUP.md</files>
  <action>
    1. coupang_manager.py line 56-58에 이미 `COUPANG_ORDER_WEBHOOK = os.getenv("COUPANG_ORDER_WEBHOOK", "")` 가 있음을 확인한다. 변경 불필요.

    2. docs/SETUP.md를 읽어서 `COUPANG_ORDER_WEBHOOK` 환경변수 설명이 없다면 환경변수 섹션에 아래 내용을 추가한다:
       ```
       COUPANG_ORDER_WEBHOOK=https://discord.com/api/webhooks/...  # 주문/배송 알림 채널 웹훅 URL
       ```
       이미 있으면 추가하지 않는다.

    3. docs/SETUP.md에 Phase 1 기능 설명이 없다면 기능 목록에 다음 한 줄을 추가한다:
       "- sync_delivery_status_to_sheet() 실행 후 상품준비중 주문 현황을 Discord embed로 자동 알림"
       이미 있으면 추가하지 않는다.
  </action>
  <verify>
    <automated>cd E:/musinsa-bot && python -c "
import re
with open('coupang_manager.py', 'r', encoding='utf-8') as f:
    src = f.read()
assert 'COUPANG_ORDER_WEBHOOK' in src, 'webhook env var missing'
print('OK: COUPANG_ORDER_WEBHOOK declared in coupang_manager.py')
"
    </automated>
  </verify>
  <done>
    - coupang_manager.py에 COUPANG_ORDER_WEBHOOK 환경변수 선언이 유지됨
    - docs/SETUP.md에 해당 변수 설명 존재 (기존 있었으면 그대로)
  </done>
</task>

</tasks>

<verification>
Task 1 완료 후 통합 검증:

```bash
cd E:/musinsa-bot && python -c "
import ast
with open('coupang_manager.py', 'r', encoding='utf-8') as f:
    src = f.read()
ast.parse(src)

# 헬퍼 함수 존재
assert '_notify_pending_preparation' in src

# 호출 존재
assert 'await _notify_pending_preparation(rows, order_status_by_id)' in src

# 0건 조기 반환 로직 존재
assert 'if not pending:' in src

# truncation 로직 존재 (MAX_FIELDS)
assert 'MAX_FIELDS' in src

# COUPANG_ORDER_WEBHOOK 사용
assert 'await post_webhook(COUPANG_ORDER_WEBHOOK' in src

print('ALL CHECKS PASSED')
"
```
</verification>

<success_criteria>
- sync_delivery_status_to_sheet() 실행 완료 직후 _notify_pending_preparation() 호출됨
- order_status_by_id 기준 "상품준비중" 상태 주문만 추출
- embed에 주문ID(field name)와 상품명(field value) 포함
- 0건이면 post_webhook 미호출
- COUPANG_ORDER_WEBHOOK 웹훅으로 전송
- 25건 초과 시 24건 + "외 N건 더" truncation 처리
- 파이썬 구문 오류 없음 (ast.parse 통과)
</success_criteria>

<output>
After completion, create `.planning/phases/01-discord/01-01-SUMMARY.md`
</output>
