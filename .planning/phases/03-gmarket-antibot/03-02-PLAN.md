---
phase: 03-gmarket-antibot
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - tests/test_stealth_regression.py
  - adapters.py
autonomous: false
requirements:
  - ABOT-03
  - GFIX-01

must_haves:
  truths:
    - "무신사, 11번가, 29CM, 옥션 어댑터가 stealth 적용 후에도 기존과 동일하게 가격을 추출한다"
    - "지마켓 상품의 가격이 기존 셀렉터로 정상 추출된다"
    - "다른 어댑터의 _after_goto hook이 no-op이어서 동작에 영향을 주지 않는다"
  artifacts:
    - path: "tests/test_stealth_regression.py"
      provides: "Regression tests for all adapters after stealth changes"
      min_lines: 60
  key_links:
    - from: "adapters.py"
      to: "config.py"
      via: "BaseAdapter._after_goto is no-op, only GmarketAdapter overrides"
      pattern: "_after_goto"
    - from: "tests/test_stealth_regression.py"
      to: "adapters.py"
      via: "Tests each adapter's _do_extract still produces correct ExtractionResult"
      pattern: "_do_extract"
---

<objective>
stealth 설정 적용 후 모든 어댑터의 회귀 테스트를 수행하고, 지마켓 가격 추출이 정상 동작하는지 검증한다.

Purpose: stealth 변경이 기존 어댑터를 깨뜨리지 않고, 지마켓 가격 추출이 실제로 복구되었음을 확인한다.
Output: 회귀 테스트 파일, 실제 지마켓 페이지 접속 확인.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-gmarket-antibot/03-01-SUMMARY.md

@adapters.py (lines 260-460 -- BaseAdapter with _after_goto hook)
@adapters.py (lines 703-873 -- GmarketAdapter)
@config.py (STEALTH constants from Plan 01)
@tests/test_adapter_site_extractors.py (existing adapter test patterns)

<interfaces>
<!-- From Plan 01 outputs -->

From config.py (after Plan 01):
```python
STEALTH_CHROME_ARGS = [
    "--no-sandbox",
    "--disable-blink-features=AutomationControlled",
    "--disable-features=AutomationControlled",
    "--disable-dev-shm-usage",
]
STEALTH_USER_AGENT = "Mozilla/5.0 (Windows NT 10.0; Win64; x64) ..."
STEALTH_INIT_SCRIPT = """
Object.defineProperty(navigator, 'webdriver', { get: () => false });
...
"""
CLOUDFLARE_CHALLENGE_WAIT_MS = 15000
```

From adapters.py (after Plan 01):
```python
class BaseAdapter:
    async def _after_goto(self, page, url: str) -> None:
        """Hook for post-navigation processing. Override in subclasses."""
        pass

class GmarketAdapter(BaseAdapter):
    _retry_on_timeout = 2  # 3 total attempts

    async def _after_goto(self, page, url: str) -> None:
        if not await self._wait_for_cloudflare_challenge(page):
            _log_price.warning(...)

    async def _wait_for_cloudflare_challenge(self, page, timeout_ms=None) -> bool:
        ...
```

From adapters.py (existing adapters):
```python
class MusinsaAdapter(BaseAdapter): ...
class TwentyNineCMAdapter(BaseAdapter): ...
class AuctionAdapter(BaseAdapter): ...
class ElevenStAdapter(BaseAdapter): ...
class UniversalAdapter(BaseAdapter): ...
```

From tests/test_adapter_site_extractors.py (test pattern):
```python
class _FakePage:
    def __init__(self, *, body_text="", visible_selectors=None, locator_texts=None, locator_attrs=None): ...
    async def goto(self, url, wait_until="domcontentloaded", timeout=None): ...
    async def wait_for_selector(self, selector, state="visible", timeout=None): ...
    def locator(self, selector): ...

class _FakeLocator:
    async def inner_text(self): ...
    async def all_text_contents(self): ...
    async def count(self): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 전체 어댑터 회귀 테스트</name>
  <files>tests/test_stealth_regression.py</files>
  <behavior>
    - Test 1: MusinsaAdapter._do_extract returns price ExtractionResult with correct value when selector has price text
    - Test 2: GmarketAdapter._do_extract returns price ExtractionResult when #itemcase_basic coupon selector has price
    - Test 3: TwentyNineCMAdapter._do_extract returns price ExtractionResult when #pdp_product_price has price
    - Test 4: AuctionAdapter._do_extract returns price ExtractionResult when selector has price
    - Test 5: ElevenStAdapter._do_extract returns price ExtractionResult when selector has price
    - Test 6: All non-Gmarket adapters have _after_goto that is the BaseAdapter default (no-op)
    - Test 7: GmarketAdapter._after_goto is NOT the BaseAdapter default (overridden)
  </behavior>
  <action>
    Create `tests/test_stealth_regression.py` that verifies every adapter's `_do_extract` still works correctly after the stealth changes from Plan 01.

    For each adapter (MusinsaAdapter, GmarketAdapter, TwentyNineCMAdapter, AuctionAdapter, ElevenStAdapter):
    1. Instantiate the adapter with `_sleep_after_load=0` and `_network_idle_before_retry=False` (to skip waits in test)
    2. Create a `_FakePage` with the adapter's exact price selector populated with a known price text (e.g., "15,000")
    3. For GmarketAdapter specifically: also populate `#itemcase_basic` in visible_selectors so `_wait_for_cloudflare_challenge` succeeds
    4. Run `asyncio.run(ad._do_extract(page, url))` and assert result == ExtractionResult("price", 15000)

    Also test that `_after_goto` is properly inherited:
    - For non-Gmarket adapters: `type(ad)._after_goto is BaseAdapter._after_goto`
    - For GmarketAdapter: `type(ad)._after_goto is not BaseAdapter._after_goto`

    Use the `_FakePage` and `_FakeLocator` patterns from `tests/test_adapter_site_extractors.py`. Import them or recreate them locally (check if they are importable from conftest or test file).

    IMPORTANT: For GmarketAdapter tests, the _FakePage must handle `page.wait_for_selector("#itemcase_basic", state="attached", ...)` -- add "#itemcase_basic" to visible_selectors so it does not raise TimeoutError.
  </action>
  <verify>
    <automated>cd "E:/musinsa-bot" && python -m pytest tests/test_stealth_regression.py -x -v</automated>
  </verify>
  <done>
    - Every adapter (Musinsa, Gmarket, 29CM, Auction, 11st) has a regression test confirming _do_extract returns correct price
    - _after_goto hook confirmed as no-op for non-Gmarket adapters
    - _after_goto hook confirmed as overridden for GmarketAdapter
    - All tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 실제 지마켓 페이지 Cloudflare 우회 검증</name>
  <action>
    Stealth 브라우저 설정 + Cloudflare challenge 대기 로직이 적용된 봇이 실제 지마켓 상품 페이지에서 가격을 추출하는지 검증.

    Plan 01에서 적용한 변경사항:
    - navigator.webdriver = false 설정
    - --disable-blink-features=AutomationControlled 크롬 플래그
    - GmarketAdapter: #itemcase_basic 대기 (15초 타임아웃)
    - 재시도 3회
  </action>
  <verify>
    1. 터미널에서 실행:
       ```
       python -c "
       import asyncio
       from playwright.async_api import async_playwright
       from config import STEALTH_CHROME_ARGS, STEALTH_USER_AGENT, STEALTH_INIT_SCRIPT

       async def test():
           async with async_playwright() as pw:
               browser = await pw.chromium.launch(headless=True, args=STEALTH_CHROME_ARGS)
               ctx = await browser.new_context(user_agent=STEALTH_USER_AGENT, timezone_id='Asia/Seoul', locale='ko-KR')
               await ctx.add_init_script(STEALTH_INIT_SCRIPT)
               page = await ctx.new_page()
               await page.goto('https://item.gmarket.co.kr/Item?goodscode=3559411802', wait_until='domcontentloaded', timeout=45000)
               try:
                   await page.wait_for_selector('#itemcase_basic', state='attached', timeout=15000)
                   print('SUCCESS: #itemcase_basic found')
                   title = await page.title()
                   print(f'Page title: {title}')
                   body = await page.locator('body').inner_text()
                   has_cloudflare = 'just a moment' in body.lower() or 'cloudflare' in body.lower()
                   print(f'Cloudflare challenge present: {has_cloudflare}')
               except Exception as e:
                   print(f'FAIL: {e}')
                   body_text = await page.locator('body').inner_text()
                   print(f'Body preview: {body_text[:500]}')
               await browser.close()

       asyncio.run(test())
       "
       ```

    2. 기대 결과:
       - "SUCCESS: #itemcase_basic found" 출력
       - "Cloudflare challenge present: False" 출력
       - Page title에 상품명 표시

    3. (실패 시) Cloudflare가 여전히 차단하면:
       - "FAIL: ..." 메시지와 Body preview에 "Just a moment" 또는 Cloudflare 관련 텍스트 표시
       - 이 경우 추가 stealth 기법 필요 (headless=False + xvfb, 또는 프록시)
  </verify>
  <done>
    - 실제 지마켓 상품 페이지에서 #itemcase_basic 셀렉터가 로드됨
    - Cloudflare challenge 텍스트가 페이지에 없음
    - 사용자가 "approved" 확인
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/ -x -v` -- all tests pass (existing + new regression + stealth tests)
2. 실제 지마켓 페이지 접속 시 #itemcase_basic 셀렉터가 로드됨 (checkpoint에서 수동 확인)
3. `grep -c "_after_goto" adapters.py` -- BaseAdapter에 hook 정의, GmarketAdapter에 override 존재
</verification>

<success_criteria>
- 5개 어댑터(Musinsa, Gmarket, 29CM, Auction, 11st) 모두 회귀 테스트 통과
- _after_goto hook이 non-Gmarket 어댑터에 영향 없음을 테스트로 증명
- 실제 지마켓 상품 페이지에서 Cloudflare challenge 없이 콘텐츠 로드 확인 (human-verify)
</success_criteria>

<output>
After completion, create `.planning/phases/03-gmarket-antibot/03-02-SUMMARY.md`
</output>
